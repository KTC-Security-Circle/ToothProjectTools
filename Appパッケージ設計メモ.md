# Appパッケージ設計メモ

## 1. ゲームループ構造

### 基本フロー

`App` クラスは典型的な **ゲームループ構造**を採用しています。

1. **processInput**

   * OpenCV HighGUI からのキー入力やマウスイベントを収集。
   * 入力は即時処理せず、**コマンドキューに積む**形に統一。

2. **update**

   * ゲーム/アプリの状態更新。
   * 今は未実装だが、アニメーションや内部状態管理を入れる余地。

3. **render**

   * `cv::imshow` 等による描画処理。
   * `skip_render_once_` フラグで「このフレームは描画を省略する」制御を可能に。

4. **dispatch**

   * コマンドキューから取り出した `DispatchCmd` を解決し、宛先（ウィンドウ/アプリ全体）へ適用。

このループにより「入力→状態更新→描画」という**一貫した処理の流れ**が確立されます。

---

## 2. コマンドキュー方式

### 採用理由

* **入力と処理を分離**：キー入力を受けた時点で即ウィンドウ操作を行うのではなく、コマンドとしてキューに積む。
* **非同期性の吸収**：複数の入力が同時に来ても逐次処理できる。
* **拡張性**：新しいコマンドを追加する場合も `Command` バリアントに加えるだけ。

### データ構造

```cpp
std::deque<DispatchCmd> cmd_que_;
```

* `DispatchCmd` = `Target` + `Command` の組み合わせ。
* `Target` は `TargetAll`, `TargetFocused`, `TargetById` など「宛先」を表す。
* `Command` は `CmdQuit`, `CmdToggleFullscreen`, `CmdMoveToMonitor`, `CmdFocusNext` など「実際の処理」を表す。

---

## 3. エンベロープ方式（Envelope）

ここでの **エンベロープ方式** とは、**「宛先(Target)」と「命令(Command)」をひとまとめにして転送する仕組み** を指します。

* **Envelope = DispatchCmd**

  * 宛先（Target）: どのウィンドウに適用するか
  * 中身（Command）: 何を実行するか

このように分離することで：

* **送信者は「何をどこに送るか」だけ指定すればよい**
  → 実際の適用は `dispatch` 側で責務分担。
* **処理経路が単純化**
  → すべてのコマンドが同じインターフェース（`DispatchCmd`）で表現可能。
* **将来の拡張性**
  → ネットワーク越し転送やスクリプト入力にも流用しやすい。

---

## 4. メソッド分割の意義

最初の `dispatch` は肥大化していましたが、以下のように分割しました：

* `handleAppLevelCommand_` : アプリ全体に作用するコマンド（例: フォーカス移動）
* `applyCommandToWindow_` : 単一ウィンドウにコマンド適用
* `dispatchToAll_` : 全ウィンドウ宛
* `dispatchToFocused_` : フォーカスウィンドウ宛
* `dispatchToId_` : 指定IDのウィンドウ宛
* `finalizeDispatch_` : 後処理（`cv::waitKey(1)` + `skip_render_once_`）

これにより、責務が明確になり、将来コマンドや宛先を増やす場合も**関数追加で拡張**できる形に整理できました。

---

## 5. 今後の発展の方向性

* **スレッドセーフなコマンドキュー**

  * 入力スレッドと描画スレッドを分ける場合、`std::mutex` や lock-free queue が必要。
* **コマンド階層の拡張**

  * 例えば「WindowGroup」や「LayoutManager」などを Target に追加可能。
* **非同期イベントとの統合**

  * ネットワーク入力やタイマーイベントも `DispatchCmd` に統一できる。
* **スクリプト/リプレイ**

  * コマンド列を保存してリプレイ → デバッグやテストに有効。

---

## まとめ

* ゲームループ構造により処理の流れを統一。
* コマンドキューで「入力と処理」を分離、非同期性と拡張性を確保。
* エンベロープ方式で「宛先」と「命令」をひとまとめにし、柔軟な拡張を可能に。
* メソッド分割により責務を明確化し、保守性・見通しを改善。

この設計は **ゲームエンジン的なアプリ構造**に近く、将来マルチウィンドウ制御やリアルタイム処理を拡張しても破綻しにくい基盤になっています。

---

👉 いしころさんの今後の開発で「コマンドをネットワーク越しに送る」「スクリプトからウィンドウを操作する」といった用途にも直結できる形になっています。

---

ご要望に合わせて、この解説を **図解（シーケンス図やデータフロー図）** 付きに整えますか？
